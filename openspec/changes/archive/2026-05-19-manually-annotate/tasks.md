## 1. Pencil 设计与交互定稿

- [x] 1.1 更新 `design/pages/admin-plan-list.pen`，在管理员计划列表中展示计划标注类型，并确保对比/手动模式可快速区分。
- [x] 1.2 更新 `design/pages/admin-plan-detail.pen`，补充手动模式的完成概览、手动标注明细和 `no_issue` 完成行展示。
- [x] 1.3 更新 `design/pages/operator.pen`，补充操作员计划列表中的标注类型展示，以及手动两栏标注工作台。
- [x] 1.4 在手动工作台设计中明确左侧病历原文黄色高亮、右侧条目卡片纵向对齐、卡片防重叠平铺和长内容滚动行为。
- [x] 1.5 在手动条目卡片设计中明确表单项：原文片段只读、质控规则单选、修改建议必填、备注选填；不展示 offset 数值。
- [x] 1.6 在手动提交设计中明确统一“提交”按钮和二次确认弹窗文案：N>0 与 N=0 两种文案。
- [x] 1.7 完成 Pencil 视觉验收，确认页面使用现有设计系统组件和 token，不引入独立视觉风格。
- [x] 1.8 按最新澄清调整 `design/pages/admin-plan-detail.pen`：手动标注明细列表改为按住院号/病例完成记录聚合展示，移除原文片段、质控规则/修改建议/备注列，标注时间使用病例完整提交时间。
- [x] 1.9 补充管理员查看手动标注详情的只读工作台视图：左侧病历原文高亮，右侧标注结果卡片纵向对齐且不可新增、编辑、删除或提交。
- [x] 1.10 补充管理员查看手动标注详情的列表视图 tab：按问题条目展示病历原文、质控规则、修改建议和条目创建时间，首期不提供详情内搜索筛选。
- [x] 1.11 修复 `design/pages/admin-plan-list.pen` 管理员计划列表示例数据，不再展示草稿状态计划，并同步项目规范中的计划状态口径。
- [x] 1.12 修复 `design/pages/admin-plan-detail.pen` 手动模式完成概览，移除首期不需要的问题类型完成情况面板，并同步规范说明。
- [x] 1.13 修复管理员手动标注详情列表视图，按 `design/components` 表格/分页样式补齐列表和翻页，并将标注时间列调整为正文样式。
- [x] 1.14 补充 `design/pages/operator.pen` 手动工作台默认态和划选后新建标注卡片态，移除右上角固定“新增条目”入口。
- [x] 1.15 修复手动工作台表单校验错误态，改为保存时在对应表单项下展示组件化错误提示。
- [x] 1.16 补充手动工作台编辑标注卡片和删除标注卡片二次确认态，并将卡片操作改为编辑/删除图标入口。
- [x] 1.17 修复手动工作台提交确认弹窗为组件化 Dialog 样式，并删除重复的手动加载失败/计划完成空态画板。

## 2. 前端 Mock 数据与类型契约

- [x] 2.1 更新前端类型定义，加入 `annotation_type`、手动任务 payload、手动条目、手动提交 payload、手动病例级明细汇总和手动标注详情类型。
- [x] 2.2 增加前端 mock 数据：对比计划、手动计划、手动任务、质控规则、手动条目、`has_issues` / `no_issue` 病例级明细汇总和单病例问题详情。
- [x] 2.3 增加 mock API 流程，支持管理员创建手动计划、上传手动 CSV 后返回导入摘要、操作员获取手动任务和提交手动标注。
- [x] 2.4 在 mock 层实现手动任务完成推进：提交后进入下一条任务，无剩余任务时返回完成态。
- [x] 2.5 在 mock 层实现重复提交保护的前端模拟，确保 UI 能处理已提交病例不可再编辑的状态。
- [x] 2.6 增加 mock offset 数据，覆盖单行片段、跨行片段、相邻片段和重复文本片段。

## 3. 前端页面与工作流实现

- [x] 3.1 更新管理员创建计划流程，增加对比/手动模式选择，并根据模式展示对应 CSV 模板要求。
- [x] 3.2 更新管理员计划列表和详情页，展示 `annotation_type`，并根据模式展示对应统计和明细结构；手动标注明细列表按住院号/病例完成记录聚合展示，不在列表展示问题条目字段。
- [x] 3.3 更新操作员计划列表，展示计划标注类型，并根据计划类型进入对比工作台或手动工作台。
- [x] 3.4 保持现有对比工作台、对比标注 modal 和对比提交 UI 兼容。
- [x] 3.5 新建手动两栏工作台组件：左侧病历原文和质控规则 popover，右侧标注条目面板。
- [x] 3.6 实现划选病历原文创建条目，自动填充只读原文片段，并在前端记录隐藏的 `start_offset` / `end_offset`。
- [x] 3.7 实现条目卡片查看、新增、编辑和删除；删除后同步移除左侧高亮并重排右侧卡片。
- [x] 3.8 实现条目表单校验：质控规则必选、修改建议必填、备注选填，offset 不展示且不可编辑。
- [x] 3.9 实现左侧黄色高亮，基于 `start_offset` / `end_offset` 将原文片段和右侧卡片建立关联。
- [x] 3.10 实现右侧卡片与左侧高亮片段的纵向对齐，并在高亮片段过近时按原文位置顺序进行非重叠平铺。
- [x] 3.11 实现统一提交按钮和二次确认弹窗：N>0 使用“当前病历已标记{N}条质控问题...”文案，N=0 使用“当前病历未标记质控问题...”文案。
- [x] 3.12 实现提交成功后的 UI 行为：当前病例条目锁定，推进下一条任务或展示计划完成状态。
- [x] 3.13 处理手动工作台加载、错误、无权限、计划关闭、计划完成、无规则和长文本滚动状态。
- [x] 3.14 实现管理员手动标注详情只读工作台视图：复用 offset 高亮、卡片纵向对齐和防重叠平铺逻辑，但隐藏新增、编辑、删除和提交能力。
- [x] 3.15 实现管理员手动标注详情列表视图 tab：按条目展示病历原文、质控规则、修改建议和条目创建时间，首期不提供详情内搜索筛选。

## 4. 前端 Mock 验收与回归

- [x] 4.1 用 mock 数据完成管理员流程验收：创建手动计划、查看计划类型、查看手动统计、查看病例级聚合标注明细和进入单病例标注详情。
- [x] 4.2 用 mock 数据完成操作员流程验收：进入手动计划、划选原文、新增/编辑/删除条目、提交 `has_issues`。
- [x] 4.3 用 mock 数据完成 `no_issue` 流程验收：0 条目提交、二次确认、进入下一条或完成态。
- [x] 4.4 验收黄色高亮、卡片纵向对齐、相邻卡片非重叠平铺和跨行片段展示。
- [x] 4.5 回归对比模式前端流程，确认对比计划列表、对比工作台和对比提交没有被破坏。
- [x] 4.6 增加或更新前端契约/交互测试，覆盖计划类型字段、手动任务 mock、手动提交 payload 和手动工作台核心交互。
- [x] 4.7 前端验收通过后，冻结前端 API 契约，作为后端实现和联调依据。
- [x] 4.8 验收管理员手动标注详情：默认只读工作台视图、列表视图 tab、`has_issues` 多条目展示和 `no_issue` 空态展示。

## 5. 后端数据模型与迁移

- [x] 5.1 在后端实体中增加计划标注类型枚举/常量，支持 `comparison` 和 `manual`，并让既有计划默认视为 `comparison`。
- [x] 5.2 为 `Plan` 增加 `annotation_type` 字段，并更新计划创建、列表、详情相关 Pydantic schema。
- [x] 5.3 调整 `CaseRecord` 的 A/B 输出和展示映射字段，使其仅对 `comparison` 计划业务必填，对 `manual` 计划可为空。
- [x] 5.4 新增手动病例完成记录模型，包含 `plan_id`、`case_id`、`operator_user_id`、`result`、病例完整提交时间、创建/更新时间和 `(case_id, operator_user_id)` 唯一约束。
- [x] 5.5 新增手动标注条目模型，包含原文片段、`start_offset`、`end_offset`、质控规则 id、规则快照、修改建议、备注、条目创建时间和必要索引。
- [x] 5.6 增加本地数据库迁移/初始化兼容逻辑，确保旧数据填充为 `comparison`，新手动表可创建。

## 6. 后端计划创建与 CSV 导入

- [x] 6.1 更新管理员创建计划接口，接收并校验 `annotation_type`，缺省时按 `comparison` 处理。
- [x] 6.2 保持 `POST /api/admin/plans` 无文件创建被拒绝，避免产生无导入数据的计划。
- [x] 6.3 将 CSV parser 拆成按模式选择的模板校验逻辑：对比模式四列，手动模式两列。
- [x] 6.4 更新导入清洗逻辑，手动模式只清洗并保存 `住院号` 和 `病历内容`，不生成 A/B 展示映射。
- [x] 6.5 保留同计划住院号去重、行级错误、导入摘要、关闭计划拒绝导入等既有语义。
- [x] 6.6 更新管理员计划列表和详情序列化结果，返回 `annotation_type` 和按模式计算的进度字段。

## 7. 后端任务队列与手动提交

- [x] 7.1 更新操作员计划列表 API，返回计划标注类型和按模式计算的进度字段。
- [x] 7.2 更新下一条任务服务，按计划类型判断当前操作员是否已完成该病例。
- [x] 7.3 为 `comparison` 任务保留现有 payload：病历、输出 A/B、展示映射和质控规则。
- [x] 7.4 为 `manual` 任务返回模式化 payload：病例 id、计划 id、住院号、病历原文和质控规则，不返回 A/B 输出和展示映射。
- [x] 7.5 新增手动标注提交 schema，支持 `result=has_issues` 与 `result=no_issue`。
- [x] 7.6 实现 `POST /api/operator/tasks/{case_id}/manual-annotate`，复用负责人和活跃计划校验。
- [x] 7.7 校验 `has_issues` 提交必须包含至少一条 entry，且每条 entry 包含 `source_text`、`start_offset`、`end_offset`、`quality_rule_id` 和 `suggestion`。
- [x] 7.8 校验 `no_issue` 提交不得包含 entries 或备注，并计为该病例完成。
- [x] 7.9 校验 offset 范围必须落在当前病例清洗后 `record_text` 内，并且对应文本与 `source_text` 在换行规范化后保持一致。
- [x] 7.10 提交问题条目时保存质控规则分类、内容、分值快照，避免历史明细受规则后续修改或删除影响。
- [x] 7.11 对同一病例同一操作员的重复手动提交返回 `409 ANNOTATION_ALREADY_EXISTS`，且不改动既有完成记录和条目。

## 8. 后端统计、明细与测试

- [x] 8.1 更新计划进度计算，使 `comparison` 统计对比标注记录，`manual` 统计手动病例完成记录。
- [x] 8.2 更新 `GET /api/admin/plans/{plan_id}/stats`，对比模式保留决策/原因分布，手动模式首期返回完成概览。
- [x] 8.3 更新 `GET /api/admin/plans/{plan_id}/annotations`，按计划类型返回对比明细或手动病例级完成汇总列表。
- [x] 8.4 手动明细支持按操作员和标注日期过滤，标注日期按病例完整提交时间计算，并沿用 `Asia/Shanghai` 日期边界语义。
- [x] 8.5 手动 `has_issues` 明细列表按病例完成记录返回一行，包含住院号、操作员、完成状态/结果、问题数、病例完整提交时间和详情标识，不在列表展示原文片段、规则快照、修改建议、备注或 offset 数值。
- [x] 8.6 手动 `no_issue` 明细列表返回可见完成行，显示 `result=no_issue`，问题数为 0，并不展示原文片段、规则、修改建议、备注或 offset 数值。
- [x] 8.7 增加后端测试：创建对比/手动计划、模板校验、错误模板拒绝、手动导入无需 A/B 输出。
- [x] 8.8 增加后端测试：手动任务获取、完成态计算、非负责人/关闭计划/缺失计划错误。
- [x] 8.9 增加后端测试：`has_issues` 提交、`no_issue` 提交、重复提交冲突、offset 范围和文本一致性校验。
- [x] 8.10 增加后端测试：规则快照保存，规则后续修改或删除不影响历史手动明细。
- [x] 8.11 增加后端测试：手动统计和手动明细按操作员、日期过滤后口径一致。
- [x] 8.12 新增管理员手动标注详情接口或等价响应，返回病例原文、病例完成信息和该病例下全部问题条目。
- [x] 8.13 管理员手动标注详情条目返回原文片段、offset、规则快照、修改建议、备注和条目创建时间；列表视图由前端展示原文片段、质控规则、修改建议和条目创建时间。
- [x] 8.14 增加后端测试：一个病例多个问题在计划详情明细中只返回一行，详情接口返回全部问题条目，`no_issue` 详情返回空条目集合。

## 9. 前后端联调

- [x] 9.1 将前端 mock API 切换为真实后端 API，保持前端契约字段名和后端响应一致。
- [x] 9.2 联调管理员创建手动计划、上传手动 CSV、查看导入摘要和计划类型展示。
- [x] 9.3 联调操作员手动任务获取、条目提交、`no_issue` 提交、任务推进和完成态。
- [x] 9.4 联调管理员手动统计、病例级聚合明细和单病例详情双视图，确认计划详情列表不展示问题条目字段或 offset，详情视图可读并能恢复高亮。
- [x] 9.5 联调权限和状态边界：非负责人、关闭计划、重复提交、模板错误、offset 校验错误。
- [x] 9.6 执行对比模式全链路回归，确认原有 A/B 导入、映射、工作台、提交、统计和明细不受影响。
- [x] 9.7 修复联调发现的字段命名、错误码、状态展示和空态差异。

## 10. 部署验收与收尾

- [x] 10.1 执行 `openspec validate manually-annotate`，确保变更 artifacts 有效。
- [x] 10.2 执行前端测试、后端测试和关键回归测试，记录未覆盖风险。
- [x] 10.3 构建前端与后端镜像/产物，确认生产配置下应用可启动。
- [x] 10.4 在部署环境完成冒烟验收：登录、创建手动计划、导入 CSV、手动标注、管理员查看统计、病例级明细和单病例标注详情。
- [x] 10.5 更新 `openspec/specs/SPEC.md`，加入手动标注模式的项目级概览和索引。
- [x] 10.6 检查并更新 README 或部署文档中涉及 CSV 模板、计划类型、测试命令和部署步骤的说明。
- [x] 10.7 整理发布说明，明确本次新增手动模式、保留对比模式兼容、首期不支持提交后编辑/重提。
