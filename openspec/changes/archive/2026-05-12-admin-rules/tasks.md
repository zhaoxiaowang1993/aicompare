## 0. 范围与基线确认

- [x] 0.1 重新阅读 `proposal.md`、`design.md` 和 `specs/admin-rules-management/spec.md`，确认本期不包含操作员标注页规则展示，也不修改 `design/pages/operator-annotate.pen`。
- [x] 0.2 检查当前前端管理员布局、路由保护、`frontend/src/components/` 下的组件封装，以及现有 `design/pages/admin-rules.pen`。
- [x] 0.3 检查当前后端旧版 `Rule` 模型、`backend/app/routers/admin_rules.py`、`backend/app/schemas/rule.py` 和数据库初始化逻辑，梳理所有旧规则结构引用。

## 1. Pencil 设计

- [x] 1.1 基于现有设计系统和组件库更新 `design/pages/admin-rules.pen`。
- [x] 1.2 设计管理员规则列表状态，包含标题、规则内容关键词搜索、规则分类筛选、分页表格、行操作、空状态、加载状态和错误状态。
- [x] 1.3 设计新增/编辑规则表单，包含规则分类单选、规则内容文本域、分值文本输入、校验提示、取消按钮和提交按钮。
- [x] 1.4 设计软删除确认流程，以及删除后规则立即从列表消失的状态。
- [x] 1.5 设计批量导入面板，包含单一批量导入入口、CSV 模板下载、CSV 文件上传、上传校验提示、导入中状态和导入结果汇总。
- [x] 1.6 设计行级导入错误展示，覆盖缺少必填项和规则分类不在枚举范围内两类错误。
- [x] 1.7 验证 Pencil 页面没有引入操作员标注页变更。

## 2. 前端 Mock 实现

- [x] 2.1 新增 `frontend/src/types/rules.ts`，定义规则分类、规则项、列表参数、创建/更新载荷、导入汇总和导入行错误类型。
- [x] 2.2 新增 mock 规则数据，覆盖五种规则分类、空列表、长规则内容、重复规则、软删除数据、导入成功和部分导入失败。
- [x] 2.3 新增 mock 版管理员规则 API 模块，提供稳定的列表、创建、更新、软删除、模板下载和导入函数签名。
- [x] 2.4 在 `frontend/src/app/App.tsx` 中新增 `/admin/rules` 路由，并使用 `ProtectedRoute role="admin"` 保护。
- [x] 2.5 更新 `AdminShell`，让规则导航项链接到 `/admin/rules`，并使用 `activeKey="rules"` 展示选中状态。
- [x] 2.6 实现 `frontend/src/pages/admin/admin-rules-page.tsx`，优先使用 `frontend/src/components/` 中已有的按钮、表格、表单控件、上传、弹窗/抽屉、空状态和反馈组件。
- [x] 2.7 在 mock 流程中实现规则内容模糊搜索和规则分类筛选，不支持按分值搜索或筛选。
- [x] 2.8 实现新增和编辑流程，包含必填校验和规则分类枚举校验。
- [x] 2.9 实现 mock 状态下的软删除行为，确保删除后的规则立即从列表消失。
- [x] 2.10 实现批量导入面板，确保 CSV 模板下载和文件上传在同一个流程中完成。
- [x] 2.11 实现 mock 导入结果处理，覆盖成功行、失败行、缺少必填项错误和非法分类错误。
- [x] 2.12 确认前端 mock 阶段没有修改操作员标注页代码或 UI。

## 3. 前端设计稿审计

- [x] 3.1 启动前端开发服务器，并以管理员登录后打开 `/admin/rules`。
- [x] 3.2 截取桌面端和窄屏视口下的列表、空状态、新增/编辑、删除确认、批量导入、导入成功和部分导入失败状态。
- [x] 3.3 将截图与 `design/pages/admin-rules.pen` 对比，检查布局、间距、字号、颜色、表格列、按钮位置、面板内容、空状态和错误状态。
- [x] 3.4 在进入后端实现前，修复所有发现的 UI 偏差。
- [x] 3.5 运行前端类型检查、lint 和可用的前端测试，验证 mock 实现。
- [x] 3.6 在实现记录或 PR 摘要中记录设计稿审计结果，再进入后端开发。

## 4. 后端数据模型与迁移

- [x] 4.1 从后端规则模型和 schema 中移除或替换旧字段 `code`、`name`、`description`、数字型 `score` 和 `is_active`。
- [x] 4.2 新增目标 `quality_rules` SQLAlchemy 模型，字段包含 `category`、`content`、文本型 `score`、`deleted_at`、`created_by`、`created_at` 和 `updated_at`。
- [x] 4.3 为规则分类、软删除时间和更新时间新增索引。
- [x] 4.4 实现本地 SQLite 重置/迁移路径，删除旧 `rules` 表并清空旧版历史规则数据。
- [x] 4.5 确保新的规则查询默认过滤 `deleted_at is null`。
- [x] 4.6 更新 bootstrap 或启动时数据库初始化逻辑，确保全新环境只创建目标规则结构。

## 5. 后端 API 与导入服务

- [x] 5.1 重构 `backend/app/schemas/rule.py`，覆盖规则列表、创建、更新、导入汇总和导入行错误响应。
- [x] 5.2 重构 `backend/app/routers/admin_rules.py`，支持带 `keyword`、`category`、`page` 和 `page_size` 的 `GET /admin/rules`。
- [x] 5.3 实现 `POST /admin/rules`，包含必填校验和规则分类枚举校验。
- [x] 5.4 实现 `PATCH /admin/rules/{rule_id}`，仅允许更新未删除规则。
- [x] 5.5 实现 `DELETE /admin/rules/{rule_id}`，通过设置 `deleted_at` 完成软删除。
- [x] 5.6 实现 `GET /admin/rules/template.csv`，返回包含规则分类、规则内容和分值表头的 CSV 模板。
- [x] 5.7 实现 `backend/app/services/rule_import.py`，包含 CSV 解析、必需表头校验、必填值校验、分类枚举校验、文本分值保留和重复数据允许。
- [x] 5.8 实现 `POST /admin/rules/import-csv`，返回总行数、成功行数、失败行数和行级错误。
- [x] 5.9 确保所有管理员规则接口都要求管理员鉴权，并且不暴露启用/停用字段。
- [x] 5.10 增加后端测试，覆盖列表/搜索/筛选/分页、创建校验、更新校验、软删除后不可见、模板下载、合法导入、缺项错误、非法分类错误和重复导入允许。

## 6. 前端真实 API 联调

- [x] 6.1 将 mock 版规则 API 调用替换为真实后端接口调用，同时保持页面调用契约不变。
- [x] 6.2 将规则列表加载接入后端分页、规则内容关键词搜索和规则分类筛选。
- [x] 6.3 将新增、编辑和软删除操作接入后端响应，并在成功后刷新列表状态。
- [x] 6.4 将 CSV 模板下载接入 `GET /admin/rules/template.csv`。
- [x] 6.5 将批量上传接入 `POST /admin/rules/import-csv`，并渲染后端返回的行级错误。
- [x] 6.6 使用现有反馈组件处理后端校验错误、鉴权错误和未知错误。
- [x] 6.7 完成真实 API 联调后，运行前端类型检查、lint 和可用测试。

## 7. 端到端验证

- [x] 7.1 基于 SQLite 本地启动后端和前端。
- [x] 7.2 验证管理员可以打开 `/admin/rules`，查看列表、新增规则、编辑规则、软删除规则，并确认删除后列表不再展示。
- [x] 7.3 验证规则内容搜索只匹配规则内容，不匹配仅出现在分值中的文本。
- [x] 7.4 验证规则分类筛选只返回所选分类下的规则。
- [x] 7.5 验证 CSV 模板可以下载并包含有效表头。
- [x] 7.6 验证合法 CSV 批量导入可以创建规则，并允许重复行。
- [x] 7.7 验证批量导入会为缺少必填项和非法分类返回行级错误。
- [x] 7.8 验证操作员和未登录用户无法访问管理员规则管理页。
- [x] 7.9 验证操作员标注页保持不变。

## 8. 部署与发布检查

- [x] 8.1 运行后端测试套件和前端检查命令。
- [x] 8.2 运行 docker-compose 构建或部署 smoke test。
- [x] 8.3 验证全新部署会创建 `quality_rules`，且不依赖旧 `rules` 表。
- [x] 8.4 验证包含旧 `rules` 表的本地 SQLite 数据会按决策被清理或删除。
- [x] 8.5 在发布说明或面向使用者的文档中记录管理员规则 CSV 模板格式和允许的规则分类值。
- [x] 8.6 准备最终验证记录，覆盖设计稿审计、前端 mock 验收、后端测试、联调检查和部署 smoke 结果。
