[project]
name = "aicompare-backend"
version = "0.1.0"
description = "FastAPI backend for AICompare"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
